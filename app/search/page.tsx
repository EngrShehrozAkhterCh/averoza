import { ProductCard } from "@/components/product-card";
import { products } from "@/lib/data";
export default async function SearchPage({ searchParams }: { searchParams: Promise<{ q?: string }> }) { const query = (await searchParams).q ?? ""; const matches = products.filter((product) => product.name.toLowerCase().includes(query.toLowerCase())); return <div className="shell"><div className="page-intro"><p className="eyebrow">Search Averoza</p><h1>Find your next thing.</h1><form className="newsletter" style={{ padding: 15, maxWidth: 560 }}><input className="search-input" name="q" defaultValue={query} placeholder="Try earbuds, lamp, wallet..." /><button type="submit">Search</button></form></div>{query && <p className="eyebrow" style={{ marginBottom: 25 }}>{matches.length} results for “{query}”</p>}{matches.length ? <div className="product-grid" style={{ paddingBottom: 100 }}>{matches.map((product) => <ProductCard product={product} key={product.id} />)}</div> : <p style={{ paddingBottom: 100 }}>No results yet. Try a broader search.</p>}</div>; }
