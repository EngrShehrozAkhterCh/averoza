import Link from "next/link";
import { getSupabaseServer } from "@/lib/supabase/server-auth";
export default async function AdminOrders() { const supabase = await getSupabaseServer(); const { data } = supabase ? await supabase.from("orders").select("id,order_number,customer_name,customer_phone,city,total,payment_method,order_status,created_at").order("created_at", { ascending: false }) : { data: [] }; return <section><div className="admin-title"><div><p className="eyebrow">Admin / fulfillment</p><h1>Orders.</h1></div></div><div className="admin-table">{data?.length ? <table><thead><tr><th>Order</th><th>Customer</th><th>Phone</th><th>City</th><th>Total</th><th>Payment</th><th>Status</th><th>Date</th></tr></thead><tbody>{data.map((order) => <tr key={order.id}><td><Link href={`/admin/orders/${order.id}`}>{order.order_number}</Link></td><td>{order.customer_name}</td><td>{order.customer_phone}</td><td>{order.city}</td><td>Rs. {Number(order.total).toLocaleString("en-PK")}</td><td>{order.payment_method}</td><td><span className="status">{order.order_status}</span></td><td>{new Date(order.created_at).toLocaleDateString("en-PK")}</td></tr>)}</tbody></table> : <p className="admin-empty">No orders yet.</p>}</div></section>; }
