import Link from "next/link";
import { notFound } from "next/navigation";
import { getSupabaseServer } from "@/lib/supabase/server-auth";
import { updateProduct } from "@/lib/admin-actions";
export default async function EditProductPage({ params }: { params: Promise<{ id: string }> }) { const supabase = await getSupabaseServer(); const id = (await params).id; const { data: product } = supabase ? await supabase.from("products").select("id,name,sku,description,price,stock_quantity,is_active").eq("id", id).single() : { data: null }; if (!product) notFound(); return <section><div className="admin-title"><div><p className="eyebrow">Admin / catalog</p><h1>Edit product.</h1></div></div><form className="admin-table admin-form" action={updateProduct}><input type="hidden" name="id" value={product.id} /><div className="field"><label htmlFor="name">Product name</label><input id="name" name="name" defaultValue={product.name} required /></div><div className="field"><label htmlFor="sku">SKU</label><input id="sku" name="sku" defaultValue={product.sku} required /></div><div className="field"><label htmlFor="price">Price (PKR)</label><input id="price" name="price" type="number" min="0" defaultValue={product.price} required /></div><div className="field"><label htmlFor="stock">Stock quantity</label><input id="stock" name="stock" type="number" min="0" defaultValue={product.stock_quantity} required /></div><div className="field"><label htmlFor="description">Description</label><textarea id="description" name="description" defaultValue={product.description ?? ""} /></div><label><input name="is_active" type="checkbox" defaultChecked={product.is_active} /> Active</label><button className="button" type="submit">Save product</button><Link className="section-link" href="/admin/products">Cancel</Link></form></section>; }
