import Link from "next/link";
import { getSupabaseServer } from "@/lib/supabase/server-auth";
import { createProduct } from "@/lib/admin-actions";
export default async function NewProductPage() { const supabase = await getSupabaseServer(); const { data: categories } = supabase ? await supabase.from("categories").select("id,name").eq("is_active", true).order("name") : { data: [] }; return <section><div className="admin-title"><div><p className="eyebrow">Admin / catalog</p><h1>New product.</h1></div></div><form className="admin-table admin-form" action={createProduct}><div className="field"><label htmlFor="name">Product name</label><input id="name" name="name" required /></div><div className="field"><label htmlFor="sku">SKU</label><input id="sku" name="sku" required /></div><div className="field"><label htmlFor="price">Price (PKR)</label><input id="price" name="price" type="number" min="0" required /></div><div className="field"><label htmlFor="stock">Stock quantity</label><input id="stock" name="stock" type="number" min="0" required /></div><div className="field"><label htmlFor="category_id">Category</label><select id="category_id" name="category_id"><option value="">Uncategorized</option>{categories?.map((category) => <option value={category.id} key={category.id}>{category.name}</option>)}</select></div><div className="field"><label htmlFor="description">Description</label><textarea id="description" name="description" /></div><button className="button" type="submit">Create product</button><Link className="section-link" href="/admin/products">Cancel</Link></form></section>; }
