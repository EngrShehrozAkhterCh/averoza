import Link from "next/link";
export default async function OrderSuccess({ searchParams }: { searchParams: Promise<{ number?: string }> }) { const number = (await searchParams).number; return <div className="shell form-page"><div className="form-panel" style={{ maxWidth: 620, margin: "0 auto", textAlign: "center" }}><p className="eyebrow">Order confirmed</p><h1>Thank you.</h1><p>Your Averoza order {number ? <strong>{number}</strong> : ""} is being prepared. We will contact you before dispatch.</p><p>Payment method: Cash on delivery.</p><Link className="button" href="/shop">Continue shopping</Link></div></div>; }
