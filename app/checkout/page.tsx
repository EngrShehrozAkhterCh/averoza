"use client";
import Link from "next/link";
import { formatPKR } from "@/lib/data";
import { useCart } from "@/components/cart-provider";
export default function CheckoutPage() { const { items } = useCart(); const subtotal = items.reduce((sum, item) => sum + item.price * item.quantity, 0); const total = subtotal + (items.length ? 200 : 0); return <div className="shell form-page"><div className="page-intro"><p className="eyebrow">Secure checkout</p><h1>Almost yours.</h1></div><div className="form-grid"><form className="form-panel"><h2>Delivery details</h2><div className="field-grid"><div className="field"><label htmlFor="name">Full name</label><input id="name" required /></div><div className="field"><label htmlFor="phone">Phone number</label><input id="phone" required /></div><div className="field full"><label htmlFor="email">Email</label><input id="email" type="email" required /></div><div className="field full"><label htmlFor="address">Address</label><input id="address" required /></div><div className="field"><label htmlFor="city">City</label><input id="city" required /></div><div className="field"><label htmlFor="province">Province</label><input id="province" required /></div><div className="field"><label htmlFor="postal">Postal code</label><input id="postal" /></div><div className="field full"><label htmlFor="notes">Order notes</label><textarea id="notes" /></div></div><h2>Payment</h2><p>Cash on delivery · You pay when your parcel arrives.</p><button className="button" type="submit">Place order · {formatPKR(total)}</button></form><aside className="summary"><h2>Your order</h2>{items.map((item) => <div className="summary-row" key={item.id}><span>{item.name} × {item.quantity}</span><strong>{formatPKR(item.price * item.quantity)}</strong></div>)}<div className="summary-row"><span>Shipping</span><strong>{formatPKR(items.length ? 200 : 0)}</strong></div><div className="summary-row summary-total"><span>Total</span><strong>{formatPKR(total)}</strong></div><Link href="/cart" className="section-link">Edit cart</Link></aside></div></div>; }
